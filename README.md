Install Brew => /usr/bin/ruby -e "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/master/install)" </br>
Install Node => brew install node </br>
Install Gatsby => npm install --global gatsby-cli </br>
</br>
npm install</br>
